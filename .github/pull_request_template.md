## Summary

<!-- What changed and why -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] `npm test` passes
- [ ] Prisma migrations are valid (`npx prisma migrate deploy`)
- [ ] No secrets or `.env` files committed

## CI Status

> **Merging is blocked until all CI checks pass.**
> The `backend-ci / test` job runs: typecheck → lint → test (with coverage).
> See the **Checks** tab below for live status.
